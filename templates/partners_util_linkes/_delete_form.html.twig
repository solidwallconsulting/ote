<form method="post" action="{{ path('app_partners_util_linkes_delete', {'id': partners_util_linke.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partners_util_linke.id) }}">
    <button class="btn">Delete</button>
</form>
