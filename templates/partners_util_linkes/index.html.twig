{% extends 'base.html.twig' %}

{% block title %}PartnersUtilLinkes index{% endblock %}

{% block body %}
    <h1>PartnersUtilLinkes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Value</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for partners_util_linke in partners_util_linkes %}
            <tr>
                <td>{{ partners_util_linke.id }}</td>
                <td>{{ partners_util_linke.name }}</td>
                <td>{{ partners_util_linke.value }}</td>
                <td>
                    <a href="{{ path('app_partners_util_linkes_show', {'id': partners_util_linke.id}) }}">show</a>
                    <a href="{{ path('app_partners_util_linkes_edit', {'id': partners_util_linke.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_partners_util_linkes_new') }}">Create new</a>
{% endblock %}
