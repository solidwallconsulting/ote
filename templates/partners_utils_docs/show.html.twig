{% extends 'base.html.twig' %}

{% block title %}PartnersUtilsDocs{% endblock %}

{% block body %}
    <h1>PartnersUtilsDocs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partners_utils_doc.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partners_utils_doc.name }}</td>
            </tr>
            <tr>
                <th>DocURL</th>
                <td>{{ partners_utils_doc.docURL }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_partners_utils_docs_index') }}">back to list</a>

    <a href="{{ path('app_partners_utils_docs_edit', {'id': partners_utils_doc.id}) }}">edit</a>

    {{ include('partners_utils_docs/_delete_form.html.twig') }}
{% endblock %}
