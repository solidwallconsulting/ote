<form method="post" action="{{ path('app_partners_utils_docs_delete', {'id': partners_utils_doc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partners_utils_doc.id) }}">
    <button class="btn">Delete</button>
</form>
